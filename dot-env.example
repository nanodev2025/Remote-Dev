# Cursor Remote Dev — Telegram Bot (.env)
# Copie ce fichier en `.env` puis remplis les valeurs.
#
# IMPORTANT
# - Ne commite jamais `.env` (il est ignoré par `.gitignore`).
# - Ne mets aucun chemin absolu personnel ici. Utilise `.` ou un chemin relatif.

# -----------------------------
# Telegram
# -----------------------------
# Token fourni par @BotFather (format: 123456789:ABCdef...)
TELEGRAM_TOKEN=

# Ton user_id Telegram (ex: via @userinfobot)
ALLOWED_USER_ID=

# PIN optionnel (recommandé) : une seconde barrière en plus du user_id.
# Usage: dans Telegram -> /pin <code>
ACCESS_PIN=

# -----------------------------
# IA (choisis UN provider)
# -----------------------------
# Options: gemini | groq | openai | anthropic | ollama
AI_PROVIDER=gemini

# Gemini (Google AI Studio)
GEMINI_API_KEY=
# Modèle conseillé free-tier (selon les comptes)
GEMINI_MODEL=models/gemini-flash-lite-latest

# Groq (si dispo)
GROQ_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Anthropic
ANTHROPIC_API_KEY=

# Ollama (local)
# OLLAMA_URL=http://localhost:11434/v1
# OLLAMA_MODEL=llama3.2

# Optionnel: limite tokens de sortie (réduit coût/latence)
AI_MAX_OUTPUT_TOKENS=2048

# -----------------------------
# Git
# -----------------------------
GIT_BRANCH=main

# URL GitHub du repo (utilisé pour générer des liens de commit dans Telegram)
GITHUB_REPO_URL=https://github.com/username/repo.git

# Dossier du repo à modifier (par défaut: dossier courant)
WORKSPACE_PATH=.

